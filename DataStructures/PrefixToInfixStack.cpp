//prefix to postfix using stack
#include <iostream>

using namespace std;
void checkSpecial() {
	for(i=0; i<n; i++){
		if()
		else
	}
				 
}
int main()
{
	cout<<"Enter the size of the prefix expression: "; int i=0, n, 
	char * arr; cin>>n;
	arr = new char[100];
	for(i=0; i<n; i++)
		cin>>arr[i];
	checkSpecial(arr, i, n);	
}
